{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/AzureTest.Web/AzureTest.Web.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}